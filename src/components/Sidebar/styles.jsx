import Button from "@mui/material/Button";
import styled from "styled-components";

const ESLink = styled(Button)``;

const Logo = styled.img`
  width: 32px;
  height: 32px;
`;

export { ESLink, Logo };
